"""Starter placeholder for EfficientNet/state-of-the-art experiments."""

from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
print("EfficientNet or other SOTA model should use the same preprocessing pipeline and split CSV files.")
print(ROOT / "results" / "tables")
