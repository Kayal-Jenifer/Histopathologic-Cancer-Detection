# Group 5 Team Handoff

## Shared foundation
Run `python src/data_loader.py --subset_size 2000` first. This generates:
- dataset summary
- train/val/test split CSVs
- class distribution screenshot
- split distribution screenshot
- sample images screenshot

## Member mapping
- Eric: data preparation, exploration, preprocessing, screenshots
- Saeed: custom CNN training, logs, graphs, saved models
- Transfer learning member: pretrained model experiments
- SOTA member: EfficientNet or similar model
- Documentation lead: combine final tables, graphs, screenshots, and report sections

## Important
Everyone should use the same generated split CSV files so comparisons are fair.
